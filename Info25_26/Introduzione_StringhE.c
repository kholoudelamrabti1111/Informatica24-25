/**/

#include <stdint.h>
#include <stdlib.h>

int maain(){
    char s{20};

    printf("Inserisci le stringhe");
    scanf("%d 19["^\n]s",s );

        printf("Quale caratterer si desidera cercare");
        scanf("%c", &car);

        for(int i=0; i<20; i++){
            if(car==s[i]){
                car++
            }
        }
}