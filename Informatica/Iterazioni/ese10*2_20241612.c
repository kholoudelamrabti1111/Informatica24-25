/*Stampa i primi N numeri dispari successivi al numero A*/

#include <stdio.h>
int main(){
    int n=0, a=0;

    do{
        printf("Inserisci un numero");
        scanf("%d", &n);
        printf("Inserisci un numero");
        scanf("%d", &a);
    }while(n<0);

    if(a%2==0){
        if(a%2==1)
    }

    for(int i=0; i<n; i++){

    }
    
}